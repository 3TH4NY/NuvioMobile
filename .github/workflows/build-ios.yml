name: Build Nuvio iOS IPA
on: [workflow_dispatch]

env:
  ORG_GRADLE_PROJECT_nuvio.android.distribution: full

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Init MPVKit submodule
        run: git submodule update --init --depth 1 -- MPVKit

      - name: Create local.properties
        run: touch local.properties

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Grant Gradle Execution Rights
        run: chmod +x gradlew

      - name: Compile and Build Xcode Project
        run: |
          cd iosApp
          xcodebuild -project iosApp.xcodeproj \
                     -scheme iosApp \
                     -configuration Debug \
                     -sdk iphoneos \
                     -derivedDataPath build \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGNING_REQUIRED=NO \
                     clean build

      - name: Create Sideloadable IPA Package
        run: |
          mkdir -p iosApp/build/Payload
          mv iosApp/build/Build/Products/Debug-iphoneos/Nuvio.app iosApp/build/Payload/Nuvio.app
          cd iosApp/build
          zip -r Nuvio.ipa Payload

      - name: Upload Binary Asset
        uses: actions/upload-artifact@v4
        with:
          name: Nuvio-Official-Build
          path: iosApp/build/Nuvio.ipa
